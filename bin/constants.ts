// set tax point for different range
export enum taxBreakpoint {
    Low = 20000,
    Mid = 40000,
    High = 80000,
    Higher = 180000
}

// set rate point for different range
export enum rateBreakpoint {
    Low = 0.1,
    Mid = 0.2,
    High = 0.3,
    Higher = 0.4
}